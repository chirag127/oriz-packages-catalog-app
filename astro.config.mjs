// astro.config.mjs — Oriz Packages catalog (custom layout, v0)
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://packages.oriz.in",
  output: "static",
});
